<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp">

    <!-- Tutorial illustration -->
    <ImageView
        android:id="@+id/imgTutorialIcon"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginBottom="32dp"
        android:importantForAccessibility="no"
        tools:src="@drawable/ic_accessibility" />

    <!-- Title -->
    <TextView
        android:id="@+id/txtTutorialTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="@android:color/white"
        android:gravity="center"
        tools:text="Gesture Navigation" />

    <!-- Description -->
    <TextView
        android:id="@+id/txtTutorialDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:textColor="@android:color/white"
        android:alpha="0.9"
        android:gravity="center"
        android:lineSpacingMultiplier="1.3"
        tools:text="Swipe left and right to navigate between sections. Double tap to select items." />

    <!-- Subtitle for deaf users (visual) -->
    <TextView
        android:id="@+id/txtTutorialSubtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:textSize="14sp"
        android:textColor="@android:color/white"
        android:alpha="0.7"
        android:gravity="center"
        android:fontFamily="sans-serif-medium"
        tools:text="For deaf users: All audio content has visual captions and indicators" />

</LinearLayout>
